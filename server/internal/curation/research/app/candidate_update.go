package app

// CandidateUpdateSize caps new candidates per research; it is not a target or minimum.
// Evaluation and images consume the actual selected list without independent caps.
const CandidateUpdateSize = 50
